import { combineReducers } from 'redux';
import initGame from './initGame';
// import playGame from './playGame';

export default combineReducers({ initGame });
// export default combineReducers({ initGame, playGame });
